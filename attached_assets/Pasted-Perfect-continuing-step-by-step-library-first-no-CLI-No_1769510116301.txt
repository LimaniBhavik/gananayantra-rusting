Perfect — continuing **step-by-step**, **library-first**, **no CLI**.
Now we refactor **Space → Escape Velocity**.

We’ll touch **only this calculator**.

---

# 🔁 NEXT STEP: LIBRARY REFACTOR → SPACE (Escape Velocity)

---

## ✅ STEP 1: LOCK THE PUBLIC API

This is what **crate users** should write:

```rust
use gananayantra_rusting::space::escape_velocity::escape_velocity;

let v = escape_velocity(0.0)?;
```

✔ No stdin
✔ No println
✔ Physics-correct
✔ Reusable in any Rust project

---

## ✅ STEP 2: PROMPT TO GIVE REPLIT (COPY–PASTE)

Paste **exactly this** into Replit AI:

```
Refactor the Space Escape Velocity calculator to be library-first.

Rules:
- This is a Rust library crate: gananayantra-rusting
- No stdin, no println, no CLI logic
- One public function only
- Return Result<f64, String>
- Add Rust doc comments (///)
- Keep physical constants private
- Input: altitude above Earth surface in meters
- Output: escape velocity in meters per second (m/s)

Only refactor:
src/space/escape_velocity.rs

Do NOT touch other modules.
```

---

## ✅ STEP 3: EXPECTED FINAL `escape_velocity.rs` (REFERENCE)

After refactor, the file **should look like this**:

```rust
const G: f64 = 6.67430e-11;        // m^3 kg^-1 s^-2
const EARTH_MASS: f64 = 5.972e24; // kg
const EARTH_RADIUS: f64 = 6_371_000.0; // meters

/// Calculates escape velocity from Earth at a given altitude.
///
/// # Arguments
/// * `altitude_m` - Altitude above Earth's surface in meters
///
/// # Returns
/// * Escape velocity in meters per second (m/s)
pub fn escape_velocity(altitude_m: f64) -> Result<f64, String> {
    if altitude_m < 0.0 {
        return Err("Altitude cannot be negative".into());
    }

    let radius = EARTH_RADIUS + altitude_m;
    let velocity = (2.0 * G * EARTH_MASS / radius).sqrt();

    Ok(velocity)
}
```

This is now **pure orbital/astrophysics library code**.

---

## ✅ STEP 4: VERIFY MODULE EXPORT

Open:

```
src/space/mod.rs
```

Ensure it contains:

```rust
pub mod orbital_velocity;
pub mod escape_velocity;
pub mod orbital_period;
```

---

## ✅ STEP 5: ADD LIBRARY UNIT TESTS

Add this **to the bottom** of `escape_velocity.rs`:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_escape_velocity_surface() {
        let v = escape_velocity(0.0).unwrap();
        assert!((v - 11186.0).abs() < 200.0);
    }

    #[test]
    fn test_invalid_altitude() {
        assert!(escape_velocity(-10.0).is_err());
    }
}
```

Run:

```bash
cargo test
```

✅ Passing = Space refactor step 2 complete.

---

## 🧠 STATUS CHECKPOINT

**Space module progress:**

| Calculator       | Status |
| ---------------- | ------ |
| Orbital Velocity | ✅      |
| Escape Velocity  | ✅      |
| Orbital Period   | ⏳ Next |

Across the crate now:

* Finance ✅
* Energy ✅
* Utilities ✅
* Geo ✅
* Water ✅
* Space (almost done) ✅

---

## 🔜 NEXT STEP (REPLY WITH ONE WORD)

Choose what we refactor next:

* **`SPACE_PERIOD_NEXT`** → orbital period
* **`STRUCTURE_LOCK`** → finalize crate layout & visibility rules
* **`REMOVE_MAIN`** → safely deprecate/remove CLI

We continue **one calculator at a time**, clean and professional.
