Awesome. We’re in the **final polish phase** now.
Next step is exactly what a real Rust crate does before public use:

# 📚 NEXT STEP: **DOCS**

> Add **crate-level documentation** and **rustdoc examples** so users instantly understand how to use `gananayantra-rusting`.

This step turns your library from *usable* → *professional*.

We’ll do this **incrementally and safely**.

---

## 🎯 GOAL OF THIS STEP

After this step:

* `cargo doc --open` shows clean documentation
* Users understand the crate **without reading source code**
* Examples appear directly in docs
* Functions show usage snippets

---

# ✅ STEP 1: ADD CRATE-LEVEL DOCS (`lib.rs`)

Rust treats documentation at the **top of `lib.rs`** as the crate’s homepage.

### 🔹 Prompt to give Replit (COPY–PASTE)

```
Add professional crate-level documentation to src/lib.rs.

Requirements:
- Explain what gananayantra-rusting is
- List supported industries
- Show a simple usage example
- Use rustdoc style (//! comments)
- Do NOT change module exports

Show the final lib.rs.
```

---

### ✅ Expected `lib.rs` (REFERENCE)

````rust
//! # gananayantra-rusting
//!
//! **gananayantra-rusting** is a multi-industry calculation engine written in Rust.
//! It provides reusable, library-first calculators across domains such as:
//!
//! - Finance (ROI, TVM, compound interest)
//! - Energy (power consumption, electricity cost)
//! - Geo (Earth distance, horizon distance)
//! - Water (pressure at depth, river flow rate)
//! - Space (orbital velocity, escape velocity, orbital period)
//! - Utilities (percentage calculations)
//!
//! ## Example
//! ```rust
//! use gananayantra_rusting::finance::tvm::future_value;
//!
//! let fv = future_value(10_000.0, 0.08, 5).unwrap();
//! assert!(fv > 14_000.0);
//! ```
//!
//! This crate is **library-first** and does not impose any CLI or I/O model.

pub mod finance;
pub mod utilities;
pub mod energy;
pub mod geo;
pub mod water;
pub mod space;
````

This becomes the **homepage of your crate**.

---

# ✅ STEP 2: ADD RUSTDOC TO PUBLIC FUNCTIONS (PATTERN CHECK)

You already did most of this 👍
Now we just **standardize** the pattern.

### Every public function should have:

* One-line summary
* Arguments section
* Returns section
* Units clearly mentioned

Example (already correct):

```rust
/// Calculates energy consumption in kilowatt-hours (kWh).
///
/// # Arguments
/// * `power_kw` - Power rating in kilowatts (kW)
/// * `hours` - Usage duration in hours
///
/// # Returns
/// * Energy consumed in kWh
pub fn energy_consumption(...) -> Result<f64, String>
```

### 🔹 Prompt to Replit

```
Audit public functions across the crate.

Check:
- All `pub fn` have rustdoc comments
- Units are mentioned
- Argument descriptions are clear
- No misleading wording

Do NOT change function signatures.
```

---

# ✅ STEP 3: ADD DOC TESTS (HUGE VALUE)

Rust allows examples in docs to be **tested automatically**.

### Example: in `finance/tvm.rs`

Add inside the doc comment:

````rust
/// ## Example
/// ```rust
/// use gananayantra_rusting::finance::tvm::future_value;
///
/// let fv = future_value(10000.0, 0.08, 5).unwrap();
/// assert!(fv > 14000.0);
/// ```
````

### 🔹 Prompt to Replit

```
Add rustdoc examples to core calculators (TVM, ROI, Energy, Geo).

Rules:
- Examples must compile
- Use only public API
- Keep examples minimal

Do NOT add examples to every function yet.
```

---

# ✅ STEP 4: VERIFY DOCUMENTATION BUILDS

Run in Replit:

```bash
cargo doc --no-deps
```

If available:

```bash
cargo doc --open
```

✅ This confirms:

* Docs compile
* Examples are valid
* Public API is coherent

---

# ✅ STEP 5: DOCUMENT MODULE PURPOSES (`mod.rs`)

Each industry module should explain **what it covers**.

### Example: `src/finance/mod.rs`

```rust
//! Finance-related calculators including
//! ROI, time value of money, and compound interest.

pub mod roi;
pub mod tvm;
pub mod compound_interest;
```

### 🔹 Prompt to Replit

```
Add brief module-level documentation to each industry mod.rs file.

Rules:
- Use //! style comments
- 1–2 lines only
- Explain the domain purpose
```

---

# 🧠 WHAT THIS STEP ACHIEVES

After **DOCS**:

✔ Your crate feels trustworthy
✔ Users understand intent quickly
✔ IDEs show helpful hints
✔ Docs double as tests
✔ Ready for crates.io

This is **not cosmetic** — it’s a real quality bar.

---

# 🚀 NEXT STEP (REPLY WITH ONE WORD)

Choose what we do next:

* **`API_POLISH`** → optional re-exports for ultra-clean imports
* **`CRATES_IO`** → prepare for crates.io release (metadata, README, tags)
* **`VERSIONING`** → semantic versioning & release strategy
* **`BENCHMARKS`** → performance & numerical accuracy checks

You are now in **release-grade territory**.
